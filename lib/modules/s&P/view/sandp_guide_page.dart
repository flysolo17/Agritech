import 'package:flutter/material.dart';

class SAndPPage extends StatelessWidget {
  const SAndPPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("S&P Guide"),
      ),
    );
  }
}
